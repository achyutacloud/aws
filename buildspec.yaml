version: 0.1
phases:
  install:
    commands:
        - aws s3 cp ./ s3://cicd-pipeline-achyuta-cloud/FileUpload/date = $(date '+%d-%m-%Y')/ --recursive --exclude "*" --exclude "MyFiles/*" --include "*.txt"

